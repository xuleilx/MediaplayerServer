/*
 * Generated by gdbus-codegen 2.40.2. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "GDBusMediaplayerServer.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.hsae.mediaplayerserver.mediaplayer
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:Mediaplayer
 * @title: Mediaplayer
 * @short_description: Generated C code for the com.hsae.mediaplayerserver.mediaplayer D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediaplayer.top_of_page">com.hsae.mediaplayerserver.mediaplayer</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.hsae.mediaplayerserver.mediaplayer ---- */

static const _ExtendedGDBusArgInfo _mediaplayer_method_info_start_OUT_ARG_ret =
{
  {
    -1,
    (gchar *) "ret",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_method_info_start_OUT_ARG_pointers[] =
{
  &_mediaplayer_method_info_start_OUT_ARG_ret,
  NULL
};

static const _ExtendedGDBusMethodInfo _mediaplayer_method_info_start =
{
  {
    -1,
    (gchar *) "start",
    NULL,
    (GDBusArgInfo **) &_mediaplayer_method_info_start_OUT_ARG_pointers,
    NULL
  },
  "handle-start",
  FALSE
};

static const _ExtendedGDBusArgInfo _mediaplayer_method_info_pause_OUT_ARG_ret =
{
  {
    -1,
    (gchar *) "ret",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_method_info_pause_OUT_ARG_pointers[] =
{
  &_mediaplayer_method_info_pause_OUT_ARG_ret,
  NULL
};

static const _ExtendedGDBusMethodInfo _mediaplayer_method_info_pause =
{
  {
    -1,
    (gchar *) "pause",
    NULL,
    (GDBusArgInfo **) &_mediaplayer_method_info_pause_OUT_ARG_pointers,
    NULL
  },
  "handle-pause",
  FALSE
};

static const _ExtendedGDBusArgInfo _mediaplayer_method_info_stop_OUT_ARG_ret =
{
  {
    -1,
    (gchar *) "ret",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_method_info_stop_OUT_ARG_pointers[] =
{
  &_mediaplayer_method_info_stop_OUT_ARG_ret,
  NULL
};

static const _ExtendedGDBusMethodInfo _mediaplayer_method_info_stop =
{
  {
    -1,
    (gchar *) "stop",
    NULL,
    (GDBusArgInfo **) &_mediaplayer_method_info_stop_OUT_ARG_pointers,
    NULL
  },
  "handle-stop",
  FALSE
};

static const _ExtendedGDBusArgInfo _mediaplayer_method_info_seek_IN_ARG_secs =
{
  {
    -1,
    (gchar *) "secs",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_method_info_seek_IN_ARG_pointers[] =
{
  &_mediaplayer_method_info_seek_IN_ARG_secs,
  NULL
};

static const _ExtendedGDBusArgInfo _mediaplayer_method_info_seek_OUT_ARG_ret =
{
  {
    -1,
    (gchar *) "ret",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_method_info_seek_OUT_ARG_pointers[] =
{
  &_mediaplayer_method_info_seek_OUT_ARG_ret,
  NULL
};

static const _ExtendedGDBusMethodInfo _mediaplayer_method_info_seek =
{
  {
    -1,
    (gchar *) "seek",
    (GDBusArgInfo **) &_mediaplayer_method_info_seek_IN_ARG_pointers,
    (GDBusArgInfo **) &_mediaplayer_method_info_seek_OUT_ARG_pointers,
    NULL
  },
  "handle-seek",
  FALSE
};

static const _ExtendedGDBusArgInfo _mediaplayer_method_info_set_rate_IN_ARG_rate =
{
  {
    -1,
    (gchar *) "rate",
    (gchar *) "d",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_method_info_set_rate_IN_ARG_pointers[] =
{
  &_mediaplayer_method_info_set_rate_IN_ARG_rate,
  NULL
};

static const _ExtendedGDBusArgInfo _mediaplayer_method_info_set_rate_OUT_ARG_ret =
{
  {
    -1,
    (gchar *) "ret",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_method_info_set_rate_OUT_ARG_pointers[] =
{
  &_mediaplayer_method_info_set_rate_OUT_ARG_ret,
  NULL
};

static const _ExtendedGDBusMethodInfo _mediaplayer_method_info_set_rate =
{
  {
    -1,
    (gchar *) "setRate",
    (GDBusArgInfo **) &_mediaplayer_method_info_set_rate_IN_ARG_pointers,
    (GDBusArgInfo **) &_mediaplayer_method_info_set_rate_OUT_ARG_pointers,
    NULL
  },
  "handle-set-rate",
  FALSE
};

static const _ExtendedGDBusArgInfo _mediaplayer_method_info_set_file_IN_ARG_filepath =
{
  {
    -1,
    (gchar *) "filepath",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_method_info_set_file_IN_ARG_pointers[] =
{
  &_mediaplayer_method_info_set_file_IN_ARG_filepath,
  NULL
};

static const _ExtendedGDBusArgInfo _mediaplayer_method_info_set_file_OUT_ARG_ret =
{
  {
    -1,
    (gchar *) "ret",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_method_info_set_file_OUT_ARG_pointers[] =
{
  &_mediaplayer_method_info_set_file_OUT_ARG_ret,
  NULL
};

static const _ExtendedGDBusMethodInfo _mediaplayer_method_info_set_file =
{
  {
    -1,
    (gchar *) "setFile",
    (GDBusArgInfo **) &_mediaplayer_method_info_set_file_IN_ARG_pointers,
    (GDBusArgInfo **) &_mediaplayer_method_info_set_file_OUT_ARG_pointers,
    NULL
  },
  "handle-set-file",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _mediaplayer_method_info_pointers[] =
{
  &_mediaplayer_method_info_start,
  &_mediaplayer_method_info_pause,
  &_mediaplayer_method_info_stop,
  &_mediaplayer_method_info_seek,
  &_mediaplayer_method_info_set_rate,
  &_mediaplayer_method_info_set_file,
  NULL
};

static const _ExtendedGDBusArgInfo _mediaplayer_signal_info_notify_progress_ARG_position =
{
  {
    -1,
    (gchar *) "position",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _mediaplayer_signal_info_notify_progress_ARG_duration =
{
  {
    -1,
    (gchar *) "duration",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_signal_info_notify_progress_ARG_pointers[] =
{
  &_mediaplayer_signal_info_notify_progress_ARG_position,
  &_mediaplayer_signal_info_notify_progress_ARG_duration,
  NULL
};

static const _ExtendedGDBusSignalInfo _mediaplayer_signal_info_notify_progress =
{
  {
    -1,
    (gchar *) "notifyProgress",
    (GDBusArgInfo **) &_mediaplayer_signal_info_notify_progress_ARG_pointers,
    NULL
  },
  "notify-progress"
};

static const _ExtendedGDBusArgInfo _mediaplayer_signal_info_notify_error_ARG_messge =
{
  {
    -1,
    (gchar *) "messge",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediaplayer_signal_info_notify_error_ARG_pointers[] =
{
  &_mediaplayer_signal_info_notify_error_ARG_messge,
  NULL
};

static const _ExtendedGDBusSignalInfo _mediaplayer_signal_info_notify_error =
{
  {
    -1,
    (gchar *) "notifyError",
    (GDBusArgInfo **) &_mediaplayer_signal_info_notify_error_ARG_pointers,
    NULL
  },
  "notify-error"
};

static const _ExtendedGDBusSignalInfo _mediaplayer_signal_info_notify_eos =
{
  {
    -1,
    (gchar *) "notifyEOS",
    NULL,
    NULL
  },
  "notify-eos"
};

static const _ExtendedGDBusSignalInfo * const _mediaplayer_signal_info_pointers[] =
{
  &_mediaplayer_signal_info_notify_progress,
  &_mediaplayer_signal_info_notify_error,
  &_mediaplayer_signal_info_notify_eos,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _mediaplayer_interface_info =
{
  {
    -1,
    (gchar *) "com.hsae.mediaplayerserver.mediaplayer",
    (GDBusMethodInfo **) &_mediaplayer_method_info_pointers,
    (GDBusSignalInfo **) &_mediaplayer_signal_info_pointers,
    NULL,
    NULL
  },
  "mediaplayer",
};


/**
 * mediaplayer_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediaplayer.top_of_page">com.hsae.mediaplayerserver.mediaplayer</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
mediaplayer_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_mediaplayer_interface_info.parent_struct;
}

/**
 * mediaplayer_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #Mediaplayer interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
mediaplayer_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * Mediaplayer:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediaplayer.top_of_page">com.hsae.mediaplayerserver.mediaplayer</link>.
 */

/**
 * MediaplayerIface:
 * @parent_iface: The parent interface.
 * @handle_pause: Handler for the #Mediaplayer::handle-pause signal.
 * @handle_seek: Handler for the #Mediaplayer::handle-seek signal.
 * @handle_set_file: Handler for the #Mediaplayer::handle-set-file signal.
 * @handle_set_rate: Handler for the #Mediaplayer::handle-set-rate signal.
 * @handle_start: Handler for the #Mediaplayer::handle-start signal.
 * @handle_stop: Handler for the #Mediaplayer::handle-stop signal.
 * @notify_eos: Handler for the #Mediaplayer::notify-eos signal.
 * @notify_error: Handler for the #Mediaplayer::notify-error signal.
 * @notify_progress: Handler for the #Mediaplayer::notify-progress signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediaplayer.top_of_page">com.hsae.mediaplayerserver.mediaplayer</link>.
 */

typedef MediaplayerIface MediaplayerInterface;
G_DEFINE_INTERFACE (Mediaplayer, mediaplayer, G_TYPE_OBJECT);

static void
mediaplayer_default_init (MediaplayerIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * Mediaplayer::handle-start:
   * @object: A #Mediaplayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.start">start()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mediaplayer_complete_start() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaplayerIface, handle_start),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Mediaplayer::handle-pause:
   * @object: A #Mediaplayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.pause">pause()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mediaplayer_complete_pause() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-pause",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaplayerIface, handle_pause),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Mediaplayer::handle-stop:
   * @object: A #Mediaplayer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.stop">stop()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mediaplayer_complete_stop() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaplayerIface, handle_stop),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Mediaplayer::handle-seek:
   * @object: A #Mediaplayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_secs: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.seek">seek()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mediaplayer_complete_seek() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-seek",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaplayerIface, handle_seek),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * Mediaplayer::handle-set-rate:
   * @object: A #Mediaplayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_rate: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.setRate">setRate()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mediaplayer_complete_set_rate() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-rate",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaplayerIface, handle_set_rate),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_DOUBLE);

  /**
   * Mediaplayer::handle-set-file:
   * @object: A #Mediaplayer.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_filepath: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.setFile">setFile()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mediaplayer_complete_set_file() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-file",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaplayerIface, handle_set_file),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /* GObject signals for received D-Bus signals: */
  /**
   * Mediaplayer::notify-progress:
   * @object: A #Mediaplayer.
   * @arg_position: Argument.
   * @arg_duration: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsae-mediaplayerserver-mediaplayer.notifyProgress">"notifyProgress"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notify-progress",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaplayerIface, notify_progress),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_UINT, G_TYPE_UINT);

  /**
   * Mediaplayer::notify-error:
   * @object: A #Mediaplayer.
   * @arg_messge: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsae-mediaplayerserver-mediaplayer.notifyError">"notifyError"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notify-error",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaplayerIface, notify_error),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

  /**
   * Mediaplayer::notify-eos:
   * @object: A #Mediaplayer.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsae-mediaplayerserver-mediaplayer.notifyEOS">"notifyEOS"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notify-eos",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaplayerIface, notify_eos),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

}

/**
 * mediaplayer_emit_notify_progress:
 * @object: A #Mediaplayer.
 * @arg_position: Argument to pass with the signal.
 * @arg_duration: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsae-mediaplayerserver-mediaplayer.notifyProgress">"notifyProgress"</link> D-Bus signal.
 */
void
mediaplayer_emit_notify_progress (
    Mediaplayer *object,
    guint arg_position,
    guint arg_duration)
{
  g_signal_emit_by_name (object, "notify-progress", arg_position, arg_duration);
}

/**
 * mediaplayer_emit_notify_error:
 * @object: A #Mediaplayer.
 * @arg_messge: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsae-mediaplayerserver-mediaplayer.notifyError">"notifyError"</link> D-Bus signal.
 */
void
mediaplayer_emit_notify_error (
    Mediaplayer *object,
    const gchar *arg_messge)
{
  g_signal_emit_by_name (object, "notify-error", arg_messge);
}

/**
 * mediaplayer_emit_notify_eos:
 * @object: A #Mediaplayer.
 *
 * Emits the <link linkend="gdbus-signal-com-hsae-mediaplayerserver-mediaplayer.notifyEOS">"notifyEOS"</link> D-Bus signal.
 */
void
mediaplayer_emit_notify_eos (
    Mediaplayer *object)
{
  g_signal_emit_by_name (object, "notify-eos");
}

/**
 * mediaplayer_call_start:
 * @proxy: A #MediaplayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.start">start()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediaplayer_call_start_finish() to get the result of the operation.
 *
 * See mediaplayer_call_start_sync() for the synchronous, blocking version of this method.
 */
void
mediaplayer_call_start (
    Mediaplayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "start",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * mediaplayer_call_start_finish:
 * @proxy: A #MediaplayerProxy.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediaplayer_call_start().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mediaplayer_call_start().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_start_finish (
    Mediaplayer *proxy,
    gboolean *out_ret,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_start_sync:
 * @proxy: A #MediaplayerProxy.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.start">start()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mediaplayer_call_start() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_start_sync (
    Mediaplayer *proxy,
    gboolean *out_ret,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "start",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_pause:
 * @proxy: A #MediaplayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.pause">pause()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediaplayer_call_pause_finish() to get the result of the operation.
 *
 * See mediaplayer_call_pause_sync() for the synchronous, blocking version of this method.
 */
void
mediaplayer_call_pause (
    Mediaplayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "pause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * mediaplayer_call_pause_finish:
 * @proxy: A #MediaplayerProxy.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediaplayer_call_pause().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mediaplayer_call_pause().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_pause_finish (
    Mediaplayer *proxy,
    gboolean *out_ret,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_pause_sync:
 * @proxy: A #MediaplayerProxy.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.pause">pause()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mediaplayer_call_pause() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_pause_sync (
    Mediaplayer *proxy,
    gboolean *out_ret,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "pause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_stop:
 * @proxy: A #MediaplayerProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.stop">stop()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediaplayer_call_stop_finish() to get the result of the operation.
 *
 * See mediaplayer_call_stop_sync() for the synchronous, blocking version of this method.
 */
void
mediaplayer_call_stop (
    Mediaplayer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "stop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * mediaplayer_call_stop_finish:
 * @proxy: A #MediaplayerProxy.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediaplayer_call_stop().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mediaplayer_call_stop().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_stop_finish (
    Mediaplayer *proxy,
    gboolean *out_ret,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_stop_sync:
 * @proxy: A #MediaplayerProxy.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.stop">stop()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mediaplayer_call_stop() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_stop_sync (
    Mediaplayer *proxy,
    gboolean *out_ret,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "stop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_seek:
 * @proxy: A #MediaplayerProxy.
 * @arg_secs: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.seek">seek()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediaplayer_call_seek_finish() to get the result of the operation.
 *
 * See mediaplayer_call_seek_sync() for the synchronous, blocking version of this method.
 */
void
mediaplayer_call_seek (
    Mediaplayer *proxy,
    guint arg_secs,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "seek",
    g_variant_new ("(u)",
                   arg_secs),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * mediaplayer_call_seek_finish:
 * @proxy: A #MediaplayerProxy.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediaplayer_call_seek().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mediaplayer_call_seek().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_seek_finish (
    Mediaplayer *proxy,
    gboolean *out_ret,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_seek_sync:
 * @proxy: A #MediaplayerProxy.
 * @arg_secs: Argument to pass with the method invocation.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.seek">seek()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mediaplayer_call_seek() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_seek_sync (
    Mediaplayer *proxy,
    guint arg_secs,
    gboolean *out_ret,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "seek",
    g_variant_new ("(u)",
                   arg_secs),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_set_rate:
 * @proxy: A #MediaplayerProxy.
 * @arg_rate: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.setRate">setRate()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediaplayer_call_set_rate_finish() to get the result of the operation.
 *
 * See mediaplayer_call_set_rate_sync() for the synchronous, blocking version of this method.
 */
void
mediaplayer_call_set_rate (
    Mediaplayer *proxy,
    gdouble arg_rate,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setRate",
    g_variant_new ("(d)",
                   arg_rate),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * mediaplayer_call_set_rate_finish:
 * @proxy: A #MediaplayerProxy.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediaplayer_call_set_rate().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mediaplayer_call_set_rate().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_set_rate_finish (
    Mediaplayer *proxy,
    gboolean *out_ret,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_set_rate_sync:
 * @proxy: A #MediaplayerProxy.
 * @arg_rate: Argument to pass with the method invocation.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.setRate">setRate()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mediaplayer_call_set_rate() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_set_rate_sync (
    Mediaplayer *proxy,
    gdouble arg_rate,
    gboolean *out_ret,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setRate",
    g_variant_new ("(d)",
                   arg_rate),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_set_file:
 * @proxy: A #MediaplayerProxy.
 * @arg_filepath: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.setFile">setFile()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediaplayer_call_set_file_finish() to get the result of the operation.
 *
 * See mediaplayer_call_set_file_sync() for the synchronous, blocking version of this method.
 */
void
mediaplayer_call_set_file (
    Mediaplayer *proxy,
    const gchar *arg_filepath,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setFile",
    g_variant_new ("(s)",
                   arg_filepath),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * mediaplayer_call_set_file_finish:
 * @proxy: A #MediaplayerProxy.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediaplayer_call_set_file().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mediaplayer_call_set_file().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_set_file_finish (
    Mediaplayer *proxy,
    gboolean *out_ret,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_call_set_file_sync:
 * @proxy: A #MediaplayerProxy.
 * @arg_filepath: Argument to pass with the method invocation.
 * @out_ret: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.setFile">setFile()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mediaplayer_call_set_file() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediaplayer_call_set_file_sync (
    Mediaplayer *proxy,
    const gchar *arg_filepath,
    gboolean *out_ret,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setFile",
    g_variant_new ("(s)",
                   arg_filepath),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ret);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediaplayer_complete_start:
 * @object: A #Mediaplayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @ret: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.start">start()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mediaplayer_complete_start (
    Mediaplayer *object,
    GDBusMethodInvocation *invocation,
    gboolean ret)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   ret));
}

/**
 * mediaplayer_complete_pause:
 * @object: A #Mediaplayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @ret: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.pause">pause()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mediaplayer_complete_pause (
    Mediaplayer *object,
    GDBusMethodInvocation *invocation,
    gboolean ret)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   ret));
}

/**
 * mediaplayer_complete_stop:
 * @object: A #Mediaplayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @ret: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.stop">stop()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mediaplayer_complete_stop (
    Mediaplayer *object,
    GDBusMethodInvocation *invocation,
    gboolean ret)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   ret));
}

/**
 * mediaplayer_complete_seek:
 * @object: A #Mediaplayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @ret: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.seek">seek()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mediaplayer_complete_seek (
    Mediaplayer *object,
    GDBusMethodInvocation *invocation,
    gboolean ret)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   ret));
}

/**
 * mediaplayer_complete_set_rate:
 * @object: A #Mediaplayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @ret: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.setRate">setRate()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mediaplayer_complete_set_rate (
    Mediaplayer *object,
    GDBusMethodInvocation *invocation,
    gboolean ret)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   ret));
}

/**
 * mediaplayer_complete_set_file:
 * @object: A #Mediaplayer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @ret: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediaplayer.setFile">setFile()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mediaplayer_complete_set_file (
    Mediaplayer *object,
    GDBusMethodInvocation *invocation,
    gboolean ret)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   ret));
}

/* ------------------------------------------------------------------------ */

/**
 * MediaplayerProxy:
 *
 * The #MediaplayerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MediaplayerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MediaplayerProxy.
 */

struct _MediaplayerProxyPrivate
{
  GData *qdata;
};

static void mediaplayer_proxy_iface_init (MediaplayerIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MediaplayerProxy, mediaplayer_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (MediaplayerProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIAPLAYER, mediaplayer_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MediaplayerProxy, mediaplayer_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIAPLAYER, mediaplayer_proxy_iface_init));

#endif
static void
mediaplayer_proxy_finalize (GObject *object)
{
  MediaplayerProxy *proxy = MEDIAPLAYER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (mediaplayer_proxy_parent_class)->finalize (object);
}

static void
mediaplayer_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mediaplayer_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mediaplayer_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_mediaplayer_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_MEDIAPLAYER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_MEDIAPLAYER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
mediaplayer_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  MediaplayerProxy *proxy = MEDIAPLAYER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mediaplayer_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mediaplayer_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
mediaplayer_proxy_init (MediaplayerProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = mediaplayer_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_MEDIAPLAYER_PROXY, MediaplayerProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), mediaplayer_interface_info ());
}

static void
mediaplayer_proxy_class_init (MediaplayerProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = mediaplayer_proxy_finalize;
  gobject_class->get_property = mediaplayer_proxy_get_property;
  gobject_class->set_property = mediaplayer_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = mediaplayer_proxy_g_signal;
  proxy_class->g_properties_changed = mediaplayer_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (MediaplayerProxyPrivate));
#endif
}

static void
mediaplayer_proxy_iface_init (MediaplayerIface *iface)
{
}

/**
 * mediaplayer_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediaplayer.top_of_page">com.hsae.mediaplayerserver.mediaplayer</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediaplayer_proxy_new_finish() to get the result of the operation.
 *
 * See mediaplayer_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
mediaplayer_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_MEDIAPLAYER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsae.mediaplayerserver.mediaplayer", NULL);
}

/**
 * mediaplayer_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediaplayer_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mediaplayer_proxy_new().
 *
 * Returns: (transfer full) (type MediaplayerProxy): The constructed proxy object or %NULL if @error is set.
 */
Mediaplayer *
mediaplayer_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MEDIAPLAYER (ret);
  else
    return NULL;
}

/**
 * mediaplayer_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediaplayer.top_of_page">com.hsae.mediaplayerserver.mediaplayer</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mediaplayer_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MediaplayerProxy): The constructed proxy object or %NULL if @error is set.
 */
Mediaplayer *
mediaplayer_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_MEDIAPLAYER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsae.mediaplayerserver.mediaplayer", NULL);
  if (ret != NULL)
    return MEDIAPLAYER (ret);
  else
    return NULL;
}


/**
 * mediaplayer_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like mediaplayer_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediaplayer_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See mediaplayer_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
mediaplayer_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_MEDIAPLAYER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsae.mediaplayerserver.mediaplayer", NULL);
}

/**
 * mediaplayer_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediaplayer_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mediaplayer_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type MediaplayerProxy): The constructed proxy object or %NULL if @error is set.
 */
Mediaplayer *
mediaplayer_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MEDIAPLAYER (ret);
  else
    return NULL;
}

/**
 * mediaplayer_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like mediaplayer_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mediaplayer_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MediaplayerProxy): The constructed proxy object or %NULL if @error is set.
 */
Mediaplayer *
mediaplayer_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_MEDIAPLAYER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsae.mediaplayerserver.mediaplayer", NULL);
  if (ret != NULL)
    return MEDIAPLAYER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * MediaplayerSkeleton:
 *
 * The #MediaplayerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MediaplayerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MediaplayerSkeleton.
 */

struct _MediaplayerSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_mediaplayer_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  MediaplayerSkeleton *skeleton = MEDIAPLAYER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_MEDIAPLAYER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_MEDIAPLAYER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_mediaplayer_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  MediaplayerSkeleton *skeleton = MEDIAPLAYER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mediaplayer_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_mediaplayer_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  MediaplayerSkeleton *skeleton = MEDIAPLAYER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mediaplayer_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _mediaplayer_skeleton_vtable =
{
  _mediaplayer_skeleton_handle_method_call,
  _mediaplayer_skeleton_handle_get_property,
  _mediaplayer_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
mediaplayer_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return mediaplayer_interface_info ();
}

static GDBusInterfaceVTable *
mediaplayer_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_mediaplayer_skeleton_vtable;
}

static GVariant *
mediaplayer_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  MediaplayerSkeleton *skeleton = MEDIAPLAYER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_mediaplayer_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _mediaplayer_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _mediaplayer_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _mediaplayer_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsae.mediaplayerserver.mediaplayer", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
mediaplayer_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_mediaplayer_on_signal_notify_progress (
    Mediaplayer *object,
    guint arg_position,
    guint arg_duration)
{
  MediaplayerSkeleton *skeleton = MEDIAPLAYER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(uu)",
                   arg_position,
                   arg_duration));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsae.mediaplayerserver.mediaplayer", "notifyProgress",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_mediaplayer_on_signal_notify_error (
    Mediaplayer *object,
    const gchar *arg_messge)
{
  MediaplayerSkeleton *skeleton = MEDIAPLAYER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s)",
                   arg_messge));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsae.mediaplayerserver.mediaplayer", "notifyError",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_mediaplayer_on_signal_notify_eos (
    Mediaplayer *object)
{
  MediaplayerSkeleton *skeleton = MEDIAPLAYER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsae.mediaplayerserver.mediaplayer", "notifyEOS",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void mediaplayer_skeleton_iface_init (MediaplayerIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MediaplayerSkeleton, mediaplayer_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (MediaplayerSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIAPLAYER, mediaplayer_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MediaplayerSkeleton, mediaplayer_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIAPLAYER, mediaplayer_skeleton_iface_init));

#endif
static void
mediaplayer_skeleton_finalize (GObject *object)
{
  MediaplayerSkeleton *skeleton = MEDIAPLAYER_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (mediaplayer_skeleton_parent_class)->finalize (object);
}

static void
mediaplayer_skeleton_init (MediaplayerSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = mediaplayer_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_MEDIAPLAYER_SKELETON, MediaplayerSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
mediaplayer_skeleton_class_init (MediaplayerSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = mediaplayer_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = mediaplayer_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = mediaplayer_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = mediaplayer_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = mediaplayer_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (MediaplayerSkeletonPrivate));
#endif
}

static void
mediaplayer_skeleton_iface_init (MediaplayerIface *iface)
{
  iface->notify_progress = _mediaplayer_on_signal_notify_progress;
  iface->notify_error = _mediaplayer_on_signal_notify_error;
  iface->notify_eos = _mediaplayer_on_signal_notify_eos;
}

/**
 * mediaplayer_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediaplayer.top_of_page">com.hsae.mediaplayerserver.mediaplayer</link>.
 *
 * Returns: (transfer full) (type MediaplayerSkeleton): The skeleton object.
 */
Mediaplayer *
mediaplayer_skeleton_new (void)
{
  return MEDIAPLAYER (g_object_new (TYPE_MEDIAPLAYER_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.hsae.mediaplayerserver.mediainfo
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:Mediainfo
 * @title: Mediainfo
 * @short_description: Generated C code for the com.hsae.mediaplayerserver.mediainfo D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediainfo.top_of_page">com.hsae.mediaplayerserver.mediainfo</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.hsae.mediaplayerserver.mediainfo ---- */

static const _ExtendedGDBusArgInfo _mediainfo_method_info_get_tags_IN_ARG_filePath =
{
  {
    -1,
    (gchar *) "filePath",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediainfo_method_info_get_tags_IN_ARG_pointers[] =
{
  &_mediainfo_method_info_get_tags_IN_ARG_filePath,
  NULL
};

static const _ExtendedGDBusArgInfo _mediainfo_method_info_get_tags_OUT_ARG_title =
{
  {
    -1,
    (gchar *) "title",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _mediainfo_method_info_get_tags_OUT_ARG_artist =
{
  {
    -1,
    (gchar *) "artist",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _mediainfo_method_info_get_tags_OUT_ARG_album =
{
  {
    -1,
    (gchar *) "album",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediainfo_method_info_get_tags_OUT_ARG_pointers[] =
{
  &_mediainfo_method_info_get_tags_OUT_ARG_title,
  &_mediainfo_method_info_get_tags_OUT_ARG_artist,
  &_mediainfo_method_info_get_tags_OUT_ARG_album,
  NULL
};

static const _ExtendedGDBusMethodInfo _mediainfo_method_info_get_tags =
{
  {
    -1,
    (gchar *) "getTags",
    (GDBusArgInfo **) &_mediainfo_method_info_get_tags_IN_ARG_pointers,
    (GDBusArgInfo **) &_mediainfo_method_info_get_tags_OUT_ARG_pointers,
    NULL
  },
  "handle-get-tags",
  FALSE
};

static const _ExtendedGDBusArgInfo _mediainfo_method_info_get_image_IN_ARG_filePath =
{
  {
    -1,
    (gchar *) "filePath",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _mediainfo_method_info_get_image_IN_ARG_imagePath =
{
  {
    -1,
    (gchar *) "imagePath",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _mediainfo_method_info_get_image_IN_ARG_pointers[] =
{
  &_mediainfo_method_info_get_image_IN_ARG_filePath,
  &_mediainfo_method_info_get_image_IN_ARG_imagePath,
  NULL
};

static const _ExtendedGDBusMethodInfo _mediainfo_method_info_get_image =
{
  {
    -1,
    (gchar *) "getImage",
    (GDBusArgInfo **) &_mediainfo_method_info_get_image_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-get-image",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _mediainfo_method_info_pointers[] =
{
  &_mediainfo_method_info_get_tags,
  &_mediainfo_method_info_get_image,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _mediainfo_interface_info =
{
  {
    -1,
    (gchar *) "com.hsae.mediaplayerserver.mediainfo",
    (GDBusMethodInfo **) &_mediainfo_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "mediainfo",
};


/**
 * mediainfo_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediainfo.top_of_page">com.hsae.mediaplayerserver.mediainfo</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
mediainfo_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_mediainfo_interface_info.parent_struct;
}

/**
 * mediainfo_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #Mediainfo interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
mediainfo_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * Mediainfo:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediainfo.top_of_page">com.hsae.mediaplayerserver.mediainfo</link>.
 */

/**
 * MediainfoIface:
 * @parent_iface: The parent interface.
 * @handle_get_image: Handler for the #Mediainfo::handle-get-image signal.
 * @handle_get_tags: Handler for the #Mediainfo::handle-get-tags signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediainfo.top_of_page">com.hsae.mediaplayerserver.mediainfo</link>.
 */

typedef MediainfoIface MediainfoInterface;
G_DEFINE_INTERFACE (Mediainfo, mediainfo, G_TYPE_OBJECT);

static void
mediainfo_default_init (MediainfoIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * Mediainfo::handle-get-tags:
   * @object: A #Mediainfo.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_filePath: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediainfo.getTags">getTags()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mediainfo_complete_get_tags() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-tags",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediainfoIface, handle_get_tags),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * Mediainfo::handle-get-image:
   * @object: A #Mediainfo.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_filePath: Argument passed by remote caller.
   * @arg_imagePath: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediainfo.getImage">getImage()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call mediainfo_complete_get_image() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-image",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediainfoIface, handle_get_image),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

}

/**
 * mediainfo_call_get_tags:
 * @proxy: A #MediainfoProxy.
 * @arg_filePath: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediainfo.getTags">getTags()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediainfo_call_get_tags_finish() to get the result of the operation.
 *
 * See mediainfo_call_get_tags_sync() for the synchronous, blocking version of this method.
 */
void
mediainfo_call_get_tags (
    Mediainfo *proxy,
    const gchar *arg_filePath,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getTags",
    g_variant_new ("(s)",
                   arg_filePath),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * mediainfo_call_get_tags_finish:
 * @proxy: A #MediainfoProxy.
 * @out_title: (out): Return location for return parameter or %NULL to ignore.
 * @out_artist: (out): Return location for return parameter or %NULL to ignore.
 * @out_album: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediainfo_call_get_tags().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mediainfo_call_get_tags().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediainfo_call_get_tags_finish (
    Mediainfo *proxy,
    gchar **out_title,
    gchar **out_artist,
    gchar **out_album,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(sss)",
                 out_title,
                 out_artist,
                 out_album);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediainfo_call_get_tags_sync:
 * @proxy: A #MediainfoProxy.
 * @arg_filePath: Argument to pass with the method invocation.
 * @out_title: (out): Return location for return parameter or %NULL to ignore.
 * @out_artist: (out): Return location for return parameter or %NULL to ignore.
 * @out_album: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediainfo.getTags">getTags()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mediainfo_call_get_tags() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediainfo_call_get_tags_sync (
    Mediainfo *proxy,
    const gchar *arg_filePath,
    gchar **out_title,
    gchar **out_artist,
    gchar **out_album,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getTags",
    g_variant_new ("(s)",
                   arg_filePath),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(sss)",
                 out_title,
                 out_artist,
                 out_album);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediainfo_call_get_image:
 * @proxy: A #MediainfoProxy.
 * @arg_filePath: Argument to pass with the method invocation.
 * @arg_imagePath: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediainfo.getImage">getImage()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediainfo_call_get_image_finish() to get the result of the operation.
 *
 * See mediainfo_call_get_image_sync() for the synchronous, blocking version of this method.
 */
void
mediainfo_call_get_image (
    Mediainfo *proxy,
    const gchar *arg_filePath,
    const gchar *arg_imagePath,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getImage",
    g_variant_new ("(ss)",
                   arg_filePath,
                   arg_imagePath),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * mediainfo_call_get_image_finish:
 * @proxy: A #MediainfoProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediainfo_call_get_image().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with mediainfo_call_get_image().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediainfo_call_get_image_finish (
    Mediainfo *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediainfo_call_get_image_sync:
 * @proxy: A #MediainfoProxy.
 * @arg_filePath: Argument to pass with the method invocation.
 * @arg_imagePath: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediainfo.getImage">getImage()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See mediainfo_call_get_image() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
mediainfo_call_get_image_sync (
    Mediainfo *proxy,
    const gchar *arg_filePath,
    const gchar *arg_imagePath,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getImage",
    g_variant_new ("(ss)",
                   arg_filePath,
                   arg_imagePath),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * mediainfo_complete_get_tags:
 * @object: A #Mediainfo.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @title: Parameter to return.
 * @artist: Parameter to return.
 * @album: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediainfo.getTags">getTags()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mediainfo_complete_get_tags (
    Mediainfo *object,
    GDBusMethodInvocation *invocation,
    const gchar *title,
    const gchar *artist,
    const gchar *album)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(sss)",
                   title,
                   artist,
                   album));
}

/**
 * mediainfo_complete_get_image:
 * @object: A #Mediainfo.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsae-mediaplayerserver-mediainfo.getImage">getImage()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
mediainfo_complete_get_image (
    Mediainfo *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * MediainfoProxy:
 *
 * The #MediainfoProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MediainfoProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MediainfoProxy.
 */

struct _MediainfoProxyPrivate
{
  GData *qdata;
};

static void mediainfo_proxy_iface_init (MediainfoIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MediainfoProxy, mediainfo_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (MediainfoProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIAINFO, mediainfo_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MediainfoProxy, mediainfo_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIAINFO, mediainfo_proxy_iface_init));

#endif
static void
mediainfo_proxy_finalize (GObject *object)
{
  MediainfoProxy *proxy = MEDIAINFO_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (mediainfo_proxy_parent_class)->finalize (object);
}

static void
mediainfo_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mediainfo_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
mediainfo_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_mediainfo_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_MEDIAINFO);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_MEDIAINFO);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
mediainfo_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  MediainfoProxy *proxy = MEDIAINFO_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mediainfo_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mediainfo_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
mediainfo_proxy_init (MediainfoProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = mediainfo_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_MEDIAINFO_PROXY, MediainfoProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), mediainfo_interface_info ());
}

static void
mediainfo_proxy_class_init (MediainfoProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = mediainfo_proxy_finalize;
  gobject_class->get_property = mediainfo_proxy_get_property;
  gobject_class->set_property = mediainfo_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = mediainfo_proxy_g_signal;
  proxy_class->g_properties_changed = mediainfo_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (MediainfoProxyPrivate));
#endif
}

static void
mediainfo_proxy_iface_init (MediainfoIface *iface)
{
}

/**
 * mediainfo_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediainfo.top_of_page">com.hsae.mediaplayerserver.mediainfo</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediainfo_proxy_new_finish() to get the result of the operation.
 *
 * See mediainfo_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
mediainfo_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_MEDIAINFO_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsae.mediaplayerserver.mediainfo", NULL);
}

/**
 * mediainfo_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediainfo_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mediainfo_proxy_new().
 *
 * Returns: (transfer full) (type MediainfoProxy): The constructed proxy object or %NULL if @error is set.
 */
Mediainfo *
mediainfo_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MEDIAINFO (ret);
  else
    return NULL;
}

/**
 * mediainfo_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediainfo.top_of_page">com.hsae.mediaplayerserver.mediainfo</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mediainfo_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MediainfoProxy): The constructed proxy object or %NULL if @error is set.
 */
Mediainfo *
mediainfo_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_MEDIAINFO_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsae.mediaplayerserver.mediainfo", NULL);
  if (ret != NULL)
    return MEDIAINFO (ret);
  else
    return NULL;
}


/**
 * mediainfo_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like mediainfo_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call mediainfo_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See mediainfo_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
mediainfo_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_MEDIAINFO_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsae.mediaplayerserver.mediainfo", NULL);
}

/**
 * mediainfo_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to mediainfo_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with mediainfo_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type MediainfoProxy): The constructed proxy object or %NULL if @error is set.
 */
Mediainfo *
mediainfo_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MEDIAINFO (ret);
  else
    return NULL;
}

/**
 * mediainfo_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like mediainfo_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See mediainfo_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MediainfoProxy): The constructed proxy object or %NULL if @error is set.
 */
Mediainfo *
mediainfo_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_MEDIAINFO_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsae.mediaplayerserver.mediainfo", NULL);
  if (ret != NULL)
    return MEDIAINFO (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * MediainfoSkeleton:
 *
 * The #MediainfoSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MediainfoSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MediainfoSkeleton.
 */

struct _MediainfoSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_mediainfo_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  MediainfoSkeleton *skeleton = MEDIAINFO_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_MEDIAINFO);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_MEDIAINFO);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_mediainfo_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  MediainfoSkeleton *skeleton = MEDIAINFO_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mediainfo_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_mediainfo_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  MediainfoSkeleton *skeleton = MEDIAINFO_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_mediainfo_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _mediainfo_skeleton_vtable =
{
  _mediainfo_skeleton_handle_method_call,
  _mediainfo_skeleton_handle_get_property,
  _mediainfo_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
mediainfo_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return mediainfo_interface_info ();
}

static GDBusInterfaceVTable *
mediainfo_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_mediainfo_skeleton_vtable;
}

static GVariant *
mediainfo_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  MediainfoSkeleton *skeleton = MEDIAINFO_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_mediainfo_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _mediainfo_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _mediainfo_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _mediainfo_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsae.mediaplayerserver.mediainfo", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
mediainfo_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void mediainfo_skeleton_iface_init (MediainfoIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MediainfoSkeleton, mediainfo_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (MediainfoSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIAINFO, mediainfo_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MediainfoSkeleton, mediainfo_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIAINFO, mediainfo_skeleton_iface_init));

#endif
static void
mediainfo_skeleton_finalize (GObject *object)
{
  MediainfoSkeleton *skeleton = MEDIAINFO_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (mediainfo_skeleton_parent_class)->finalize (object);
}

static void
mediainfo_skeleton_init (MediainfoSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = mediainfo_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_MEDIAINFO_SKELETON, MediainfoSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
mediainfo_skeleton_class_init (MediainfoSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = mediainfo_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = mediainfo_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = mediainfo_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = mediainfo_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = mediainfo_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (MediainfoSkeletonPrivate));
#endif
}

static void
mediainfo_skeleton_iface_init (MediainfoIface *iface)
{
}

/**
 * mediainfo_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-hsae-mediaplayerserver-mediainfo.top_of_page">com.hsae.mediaplayerserver.mediainfo</link>.
 *
 * Returns: (transfer full) (type MediainfoSkeleton): The skeleton object.
 */
Mediainfo *
mediainfo_skeleton_new (void)
{
  return MEDIAINFO (g_object_new (TYPE_MEDIAINFO_SKELETON, NULL));
}

